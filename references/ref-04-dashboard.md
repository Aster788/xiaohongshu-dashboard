# ref-04-dashboard — 图表视觉对标

本文件说明**仓库内**如何放置「ref-04」设计参考，供实现公开展示页 Recharts 样式时对照。

## 当前仓库状态（供协作对齐）

- 已跟踪文件：`[color-purple.md](./color-purple.md)`（色板）。
- **像素级 UI 稿**：请将导出图或设计说明保存为同目录下的 `**ref-04-dashboard.png`**（或 `.webp`），并在本文下方「变更记录」里写一句文件名与日期。仅上传到对话而未写入本目录时，其他环境（CI、同事克隆、Agent 只读仓库）**无法**看到该文件，因此会出现「计划里写找不到 ref」的情况。

## 实现顺序建议

1. 色板仍以 `color-purple.md` + PRD 为准。
2. 有 `ref-04-dashboard.png` 后，再调 `components/dashboard/`* 与 `app/globals.css` 中的网格、轴、折线粗细与卡片圆角。

## 变更记录

- （在此追加：例如 `2026-04-06` 添加 `ref-04-dashboard.png`）